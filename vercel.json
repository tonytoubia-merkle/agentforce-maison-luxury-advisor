{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/proxy?path=:path*" }
  ],
  "functions": {
    "api/proxy.js": {
      "maxDuration": 120
    }
  }
}
